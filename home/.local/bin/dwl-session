#!/bin/sh
set -eu

export XDG_CURRENT_DESKTOP=dwl
export XDG_SESSION_DESKTOP=dwl
export XDG_SESSION_TYPE=wayland

export CLAUDE_CONFIG_DIR="$HOME/Vault/Agents/.claude"
export CODEX_HOME="$HOME/Vault/Agents/.codex"

uid="$(id -u)"
runtime="/run/user/$uid"

if [ -d "$runtime" ] && [ -w "$runtime" ]; then
    XDG_RUNTIME_DIR="$runtime"
else
    XDG_RUNTIME_DIR="/tmp/xdg-runtime-$uid"
    mkdir -p "$XDG_RUNTIME_DIR"
    chmod 700 "$XDG_RUNTIME_DIR"
fi

export XDG_RUNTIME_DIR

exec dbus-run-session -- dwl -s "$HOME/.local/bin/dwl-autostart"
